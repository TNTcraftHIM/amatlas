# 建造纪律

本规则只管实现如何推进。

- 编辑前写清任务、关键假设与可观察成功条件；需求允许合理默认时，说明默认并继续。
- 从最接近的现有 example 或 module 做最小可工作路径，跑通后才增加复杂度。
- 一次只做一个完整改动，立刻跑最近的相关检查；再次改码前先读懂并解释失败。
- 内容、装配、表现、模块与 core 各归其层；陌生任务从相关 skill、example 或 reference 路由。
- 短计划用于导航，不能代替实现。
